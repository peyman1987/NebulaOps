# NebulaOps v22.3 localhost final fix report

## Changes

- Updated shell standalone buttons to open `/remotes/<mfe>/` instead of legacy public MFE 42xx URLs.
- Updated visible route labels in the shell, launcher and command palette.
- Patched both Angular source files and the already-generated `frontend/dist` bundle used by the Docker image.
- Added a project-level `.gitignore` covering Angular, Java, Python, Go, Docker, Terraform, IDE files, generated reports and local archives.
- Removed stale Markdown documents and SVGs that referenced older runtime layouts or obsolete public MFE ports.
- Added an aligned v22.3 reverse-proxy runtime SVG diagram.

## Current browser entry point

```text
http://nebulaops.localhost
```

## Current MFE standalone pattern

```text
http://nebulaops.localhost/remotes/<mfe>/
```

## Current MFE remote entry pattern

```text
http://nebulaops.localhost/remotes/<mfe>/remoteEntry.js
```
