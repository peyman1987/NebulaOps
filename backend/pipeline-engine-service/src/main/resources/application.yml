server: { port: 8087 }
spring:
  application: { name: pipeline-engine-service }
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  metrics:
    tags:
      application: pipeline-engine-service
