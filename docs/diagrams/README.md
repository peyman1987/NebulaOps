# Diagram Index

This directory contains animated SVG diagrams for the NebulaOps platform.

| File                        | Purpose                             |
|-----------------------------|-------------------------------------|
| `runtime-architecture.svg`  | Full runtime topology               |
| `messaging-cache-flow.svg`  | RabbitMQ and Redis responsibilities |
| `request-flow-sequence.svg` | End-to-end task request lifecycle   |
| `service-port-map.svg`      | Local service and port map          |
| `kubernetes-helm-view.svg`  | Kubernetes deployment structure     |
| `gitlab-argocd-flow.svg`    | CI/CD and GitOps flow               |

The diagrams use inline SVG animation and do not require external assets.
