events {}
http {
  resolver 127.0.0.11 valid=10s ipv6=off;
  resolver_timeout 5s;

  server {
    listen 8080;

    location = /__nebulaops_proxy_health {
      access_log off;
      add_header Content-Type text/plain;
      return 200 'mongo-express-basic-proxy ok\n';
    }

    location / {
      set $upstream_mongo_express http://mongo-express:8081;
      proxy_pass $upstream_mongo_express;
      proxy_http_version 1.1;
      proxy_connect_timeout 10s;
      proxy_send_timeout 60s;
      proxy_read_timeout 60s;
      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
      proxy_set_header X-Forwarded-Host $host;
      proxy_set_header Upgrade $http_upgrade;
      proxy_set_header Connection "upgrade";
      proxy_redirect off;
      # Satisfy the legacy Mongo Express basic-auth challenge after Keycloak SSO.
      proxy_set_header Authorization "Basic YWRtaW46YWRtaW4=";
      proxy_hide_header WWW-Authenticate;
      proxy_buffering off;
    }
  }
}
