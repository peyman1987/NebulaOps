# NebulaOps v22.3 Hotfix 5 — Documentation and preflight alignment

## Purpose

Hotfix 5 fixes the static preflight failures introduced by the previous documentation cleanup. The package validator still requires the canonical v22.3 SVG names and exact documentation references, so the docs were aligned rather than bypassing validation.

## Changes

- Restored `docs/architecture-animated.svg` with current same-origin reverse-proxy content.
- Rebuilt the canonical SVG set required by `scripts/validate-package.py`.
- Updated `README.md` and `docs/TECHNICAL_DOCUMENTATION.md` to reference all required diagrams.
- Added the exact validated terms for Angular, MongoDB, RabbitMQ, Helm, Grafana, WSL, Go, Redis, GitLab, Argo CD and Prometheus.
- Removed non-canonical SVGs that were not aligned with the validated diagram set.
- Kept `.gitignore` in the repository root.

## Browser contract

The supported browser entry point remains:

```text
http://nebulaops.localhost
```

Micro frontend standalone buttons and shell remote loading use same-origin `/remotes/<mfe>/` routes.
